# Visualizer Hotel Sur

First prototype for “En Cualquier Lugar”.

```bash
npm install
npm run dev
```

Place the loop at `public/videos/en-cualquier-lugar.mp4`.

Controls: `F` fullscreen, `H` debug panel, `Space` soft pulse.
